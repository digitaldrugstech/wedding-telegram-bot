# Production Environment Variables for Docker Compose
# Copy this file to .env.prod and fill with real values
# Load with: docker-compose -f docker-compose.prod.yml --env-file .env.prod up

# Prod Bot Token (from @BotFather)
TELEGRAM_BOT_TOKEN_PROD=your_prod_bot_token_here

# PostgreSQL Password
POSTGRES_PASSWORD_PROD=your_secure_postgres_password_here
